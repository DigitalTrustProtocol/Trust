The web UI needs an overhaul.

Steps:

1. Graph visualizer.

Use this project as inperiation for implementing the app web implementation.
Main https://github.com/nostr-wot

Source: https://github.com/nostr-wot/nostr-wot

Add the https://github.com/nostr-wot/nostr-wot-sdk libary in the lib folder, no seperated package, as we need full control.

Browser Extension - We will not implement this, just use it in our application as the https://github.com/nostr-wot/nostr-wot uses it.
Make sure to take advanges of all features that it provides for the nostr-wot app as well.

It is specially important to implement the playground part from https://github.com/nostr-wot/nostr-wot as this the main focus.

For now just implement all the api as in nostr-wot and use the oracle url, specified in the nostr-wot.

When done, we should be able to login by the nostr-wot extension in the browser, and navigate the playground followers etc.
Optimal, there should be no difference between nostr-wot and this app on the playground.

2. On the loggedin user dropdown button on the top right corner.
Add two menu items.


3. Graph intergration.

@documentation/implementation/GraphApi.md I need you to analyse the web app and the graph. And write a interface document for the api.
I have already defined some endpoints like resolve and privacy/vanish, activation of this functionality should be in the privacy of the loggedin account. If no privacy page are avaiable make one, that can be open from the dropdown box on top right loggin user button.
The api "identity" needs to "whoami" as its the servers pubkey.
I imagine that the graph server may implement a identity service, as keep track of pubkey, item keys (hash of content) and their names and urls and other identifiers.
However this requries the graph to also store metadata information about the pubkeys and the item keys. This has not been implemented yet. 
Pubkeys have kind 0 with meta data and other kinds, but items like a hash of an image really do not have a kind for meta data. Im not sure how to do this yet.

Also the web app will visualize the graph.
There are two views. 
3.1. One which shows all connections from an author. Both good and bad, I imagine green for 1 and red for -1.
When clicking a node it expands its connections, and closes again if open.
Each node will use the name metadata if possible.
An optional mode, is to use the icon of a pubkey, if exist otherwise the autogenerated icon based on pubkey. The icon mode is good for identification, but is heavy on resouces, therefore only optional. 
When selecting a node, a card shows detail information about the pubkey or item key.
There should be action functionality on the node card for quick trust, distrust, cancel trust, follow, mute, block etc. The follow, mute, block only applies when its a pubkey. (Subjecttype p). 
The card should also show the trust score from the perspective of the author.
Its possible from a seachable drowdown to find or use another pubkey as starting point, than the logged in account on the site. Per default it the loggin pubkey.
The seachable drowdown saves the last used pubkeys for a quick reuse. The loggin user is always first pick.
Check if the nostr-wot brower extension do have an kind 0 metadata database storage, that you could use.

The needed api here is: 
A call that returns all trusts from an author. The optional filter is trust, trenary 1,-1 and the contexts. 
The current api: trustedSubjects, needs to be renamed to "out".
And we need an api call: "in" that will return all incoming trust for specified author, with optional filter on trust trenary and contexts.
Remember that the context is heriacal, se how its done in the graph.
The out and in api call return all the trust connections from/to the author, the information includes the trust attributes data. 


3.2. Detail view of resolve of from -> to result. 
The graph network can be large therefore dedicated view of author -> subject view is needed.
This view starts from the author and only shows connections that are in the result final subject is reached.
This way can visualize the path from author to subject.
The needed api here is : resolve, that already forefill the needs for this view. Beside the icon and meta data information. 

If possible keep a undo and redo button on so we can go back and forth from graph overview to resolve results and back, without having to reload everyting again. 
This way we keep the state of the graph for which nodes that was open, so we do not need to reopen it again going back.


4. Attestr
We need to add new trust event by UI 
Here we can draw inspiration from https://attestr.xyz/
https://github.com/dadofsambonzuki/attestr

As the playground provides the navigation, attestr provides the part where we can create new Trust event on visual way just like attestr.

When done, we will be able to create new trust events and se lists of trust events issued by an author (pubkey).
